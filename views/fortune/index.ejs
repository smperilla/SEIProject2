<% fortunes.forEach(fortune=> { %>

    <h1>
        <%= fortune.name %>
    </h1>
    <p>
        <%= fortune.description %>
    </p>

    <button id="<%= fortune._id %>" class="addToOrderBtn"> Add to order</button>
    <% }); %>

        <div>
            <button id="submit">Submit Order</button>

        </div>